arr = list(map(int,input().split()))
l = len(arr)
for i in range(l):
    for j in range(l-i-1):
        if arr[j+1]<arr[j]:
            arr[j+1],arr[j] = arr[j],arr[j+1]
            
print(arr)            

time comp = O(n2)
