arr = list(map(int,input().split()))
l = len(arr)
for i in range(1,l):
    for j in range(i-1,-1,-1):
        if arr[i]<arr[j]:
            arr[i],arr[j] = arr[j],arr[i]
            
print(arr)

time comp = O(n2)
